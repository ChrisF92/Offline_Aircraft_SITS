Offline Aircraft SITS v2 — Build Instructions (Phase 0)
=========================================================

You build the .xlsm workbook locally from the .txt VBA sources in this folder.

Prerequisites
-------------
- Microsoft Excel 2016 (Office 16) with macros enabled
- Trust access to the VBA project object model (optional, for import tools):
  File > Options > Trust Center > Trust Center Settings > Macro Settings >
  check "Trust access to the VBA project object model"

Step 1 — Create a new workbook
------------------------------
1. Open Excel and create a blank workbook.
2. Save As > Excel Macro-Enabled Workbook (.xlsm)
   Example: ZZ383_SITS_v2.xlsm

Step 2 — Import VBA modules (order recommended)
-----------------------------------------------
Open the VBA editor (Alt+F11). For each file below, insert a new standard module
(Insert > Module), set the module Name property exactly as shown, and paste the
file contents.

IMPORTANT: The module Name must match the Attribute VB_Name line at the top of
each file. Module names must NOT be the same as a macro you call from another
module (e.g. do not name a module "Setup_CreateTables" — that causes a compile
error "Expected variable or procedure, not module").

Runtime modules:

  File                              Module Name (Name property)
  --------------------------------  ---------------------------
  vba/modules/CONFIG.txt            CONFIG
  vba/modules/modUtility.txt        modUtility
  vba/modules/modValidation.txt     modValidation
  vba/modules/modImportValidation.txt modImportValidation
  vba/modules/modAuditTrail.txt     modAuditTrail

Setup modules (one-time use):

  File                                  Module Name
  ------------------------------------  -------------
  vba/setup/Setup_CreateSheets.txt      modSetupSheets
  vba/setup/Setup_CreateTables.txt      modSetupTables
  vba/setup/Setup_CreateConfig.txt      modSetupConfig
  vba/setup/Setup_CreateNamedRanges.txt modSetupNames
  vba/setup/Setup_CreateDashboard.txt   modSetupDashboard
  vba/setup/Setup_RunAll.txt            modSetupRunAll

The macros you run still use their procedure names, e.g. Setup_CreateTables and
Setup_RunAll — only the module container names differ.

ThisWorkbook code:

  vba/modules/ThisWorkbook.txt        -> Paste into ThisWorkbook object (not a module)

Step 3 — Run Phase 0 setup
--------------------------
Required before running setup (compile-time dependencies):
  CONFIG, modUtility, modValidation

Import modAuditTrail before normal use (Workbook_Open / future phases).
Setup_RunAll does not call LogAction — it writes the setup audit row directly.

1. In the VBA editor, open the modSetupRunAll module.
2. Place the cursor inside Setup_RunAll and press F5.
3. Enter the aircraft tail number when prompted (e.g. ZZ383).
4. When complete, run ValidateWorkbookStructure (modValidation) to confirm.

Alternatively, run each Setup_* macro individually in this order:
  Setup_CreateSheets
  Setup_CreateTables
  Setup_CreateConfig
  Setup_CreateNamedRanges
  Setup_CreateDashboard

Step 4 — Save
-------------
Save the workbook. The Phase 0 skeleton includes:

  Visible sheets:  Dashboard, SITS, Usage, Audit Log
  Hidden sheets:   _CONFIG, _DATA_SITS, _DATA_USAGE_*, _DATA_WP*, _WP_STATE
  Tables:          tblSITS, tblUsageBaseline, tblUsageLog, tblUsageCurrent,
                   tblWorkPackages, tblWPTasks, tblConfig, tblIntervalMap, tblAudit

What Phase 0 does NOT include yet
---------------------------------
- Setup Wizard (Phase 1)
- GOLDesp file import macros (Phase 1)
- Usage entry form (Phase 2)
- SITS calculations and view (Phase 3)
- Work packages, extensions, deferrals (Phases 4–5)

Import rules (for Phase 1 reference)
------------------------------------
SITS Long Forecast — ALL required (abort if any missing):
  Interval Type Code, Life Remaining, Task Code, Task Sequence,
  Task Description, Serial, Actual Interval, Extension, End Item Serial

Usage exports (airframe + engine) — ALL required:
  Interval Type, Task Code, Life Used, Top Serial

Engine usage must include SM and SN after import (WC/STARTS/ECU).

Troubleshooting
---------------
"Expected variable or procedure, not module"
  -> Setup module Name must be modSetupRunAll etc., not Setup_RunAll.
     See module naming table in Step 2.

"Delete method of Worksheet class failed" during setup
  -> Ensure modUtility includes PrepareWorkbookForSetup (Phase0-setup-v4).
  -> Re-paste modUtility.txt and Setup_RunAll.txt, then Compile.
  -> Setup_RunAll must NOT contain: Do While Worksheets.Count > 0 ... Delete
  -> A brand-new workbook with only Sheet1 is supported (sheet is renamed,
     not deleted).

"A workbook must contain at least one visible worksheet" during setup
  -> Build marker Phase0-setup-v6: all sheets stay visible until the
     "Finalizing sheet layout" step at the end of Setup_RunAll.
  -> OrderSetupSheets skips moving a sheet before itself (error 1004).

If setup fails, the error message shows which step failed (e.g. Building sheets).
  20260706-Long Forecast_ZZ382.xlsx   — SITS export sample
  ZZ383 Item Usage.xlsx               — Airframe usage sample
  ZZ383 Engine Usage.xlsx             — Engine usage sample (includes SM/SN)
